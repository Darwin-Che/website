#!/bin/bash

PORT=8080
BIND=0.0.0.0
REMOTE=192.168.1.1

## Install Theme
if [[ ! -d themes ]]; then
	mkdir themes
	pushd themes
	git clone https://github.com/athul/archie.git
	popd
	
fi

## Start hosting
hugo server -D --bind $BIND -b $REMOTE -p $PORT

